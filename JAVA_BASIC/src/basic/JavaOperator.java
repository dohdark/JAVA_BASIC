package basic;

// 연산자
// + : 덧셈
// - : 뺄셈
// * : 곱셈
// / : 나눗셈 몫
// % : 나눗셈 나머지

// ==: 같다
// !=: 같지 않다
// ! : 반대 !True == False
// 조건 ? 값1 : 값2 -> (삼항연산자) 참일때 값1, 거짓일때 값2
// age > 19 ? "가능" : "불가"
// +=, -=, *=, /=
// ex ) total = total + num;
// total += num; 로 쓸수 있다.

// AND, OR 
// & | && || 두개를 쓸때는 뒤의 값이 결과에 영향을 미치지 않을때
// ++, -- : 증감연산자
// 문자열 연산
public class JavaOperator {

}
